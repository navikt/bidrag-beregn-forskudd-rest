BIDRAGSJABLON_URL: http://localhost:8096

springdoc:
  swagger-ui:
    path: /

spring:
  main:
    allow-bean-definition-overriding: true

no.nav.security.jwt:
  issuer:
    aad:
      discovery_url: http://localhost:${mock-oauth2-server.port}/aad/.well-known/openid-configuration
      accepted_audience: aud-localhost
